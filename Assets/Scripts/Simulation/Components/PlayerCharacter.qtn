// Component attached to player entities to track character-specific state
component PlayerCharacter
{
    bool IsGrounded; // True when the character is touching the ground, used for jump logic and animations
}
